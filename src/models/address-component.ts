export interface AddressComponent {
    // eslint-disable-next-line @typescript-eslint/naming-convention
    long_name: string;
    // eslint-disable-next-line @typescript-eslint/naming-convention
    short_name: string;
    types: Array<string>;
}
